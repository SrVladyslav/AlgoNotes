### Green flags: Multi dimentional dynamic programming

- Given a matrix, or grid: find an optimal path or sum the number of paths.
- Given two strings, compare, transform or compare.
- We need to get the maximum quantity of items to fit some weight in a backpack for example.- We need to optimize the result in every sub-problem of one sub-sequence. delete, unify, or split the elements of the sequence.
