"""
Pattern: Unidimensional dynamic programming

Time: O(n)
Memory: O(n)
"""


def climb_stairs(n: int) -> int:
    # =============================================================================
    # Stage 1: Initialization or the DP array
    # =============================================================================
    dp: list[int] = [0] * (n + 1)

    # =============================================================================
    # Stage 2: Process the base cases
    # =============================================================================
    dp[0] = 1
    dp[1] = 1

    for i in range(2, n + 1):
        # =============================================================================
        # Stage 3: Process recursively the relations
        # =============================================================================
        ...

    return dp[n]
