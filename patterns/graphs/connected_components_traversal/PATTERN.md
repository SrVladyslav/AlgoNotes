## Green flags traversal connected components

- We need to find the number of connected components in the graph.
- We need to find the maximum/minmum components with some property (e.g. Components with the largest sum of values).
