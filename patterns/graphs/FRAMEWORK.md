- Begin
  |
  |
  if (Obtain the shortest path) -> Pattern shortest path (BFS if edged with same weight) else Another algorithm e.g. Dijkstra
  |
  |
  elif (Problem to check for cycles/topological order) -> Topological sorting/ dependency order
  |
  |
  else -> Pattern Connected components traversal
